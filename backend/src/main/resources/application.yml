server:
  port: ${PORT:8080}
spring:
  application:
    name: nisa-ai-commerce-engine
springdoc:
  swagger-ui:
    path: /swagger-ui.html
nisa:
  security:
    jwt-secret: ${NISA_JWT_SECRET:}
  cors:
    allowed-origins: ${NISA_ALLOWED_ORIGINS:http://127.0.0.1:5175,http://localhost:5175}
